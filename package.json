{
  "name": "@betomorrow/expo-plugins",
  "description": "Simple Expo plugins for common use cases",
  "packageManager": "yarn@4.3.1",
  "private": true,
  "scripts": {
    "prepack": "yarn workspaces foreach -Apt run prepack"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^8.1.7"
  }
}
